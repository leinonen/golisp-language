(ns defmethod)

(definterface Stringer
  (String [] -> string))

(defstruct Point
  x float64
  y float64)

(defmethod *Point String [p] -> string
  (str (.-X p) "," (.-Y p)))

(defstruct Rect
  width float64
  height float64)

(defmethod Rect Area [r] -> float64
  (* (.-Width r) (.-Height r)))
