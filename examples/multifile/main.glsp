(ns main (:import [fmt]))

(defn main []
  (fmt/println (greet "World"))
  (fmt/println (greet "glisp"))
  (let [nums (map (fn [n] (square (int n))) (range 1 6))]
    (print-results "squares" nums)))
