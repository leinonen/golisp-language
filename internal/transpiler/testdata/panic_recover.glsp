(ns main)

(defn safe-divide [a int b int] -> any
  (defer (recover))
  (when (= b 0)
    (panic "division by zero"))
  (/ a b))

(defn validate [s string]
  (when (= s "")
    (panic "empty string not allowed"))
  s)
