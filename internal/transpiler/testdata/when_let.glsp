(defn notify [users any id any] -> any
  (when-let [user (get users id)]
    (fmt/println "found" (:name user))
    (:name user)))

(defn first-tag [post any] -> any
  (when-let [[tag _] (get post "tags")]
    tag))
